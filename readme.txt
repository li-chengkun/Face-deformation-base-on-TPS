环境是VS2012，OpenCV 3.0.0
配置了OpenCV库和于仕祺老师的Facedetect库，需要添加opencv_world300.dll和libfacedetect.dll。
不过在debug环境下需要添加opencv_world300d.dll。
不需要关键点的txt文件，程序会自动检测人脸的关键点。
程序执行之后只需要按照提示进行即可。
输出结果放了三张，分别对应三次插值结果
